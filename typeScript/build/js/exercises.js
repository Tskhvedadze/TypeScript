"use strict";
/*

function isReallyNaN(val: any): boolean {
   return Number.isNaN(val)
}

console.log(isReallyNaN(NaN))
console.log(isReallyNaN(undefined))

function arr2bin(arr: any[]): string {
    return arr
        .filter((i) => typeof i === 'number')
        .reduce((a, b) => a + b, 0)
        .toString(2)
}

console.log(arr2bin([1, 2, 3, 4, 5]))
console.log(arr2bin([1.234, 2.233, 'a']))

console.log(arr2bin([true, true, false, 15]))


function cubeChecker(volume: number, side: number): boolean {
    if (Math.sqrt(volume / side) === side) return true
    else return false
}

console.log(cubeChecker(8, 2))
console.log(cubeChecker(27, 3))


const multipleOfIndex = (array: number[]): number[] =>
    array.filter((el, i) => el % i === 0)

console.log(multipleOfIndex([22, -6, 32, 82, 9, 25]))

*/
